---
phase: 18-architecture-documentation-lite-code-linked-diagrams-of-current-codebase-state-for-human-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ARCHITECTURE.md
autonomous: false
requirements:
  - ARCH-18-01

must_haves:
  truths:
    - "A human can orient themselves in the codebase within 10 minutes by reading ARCHITECTURE.md"
    - "Every diagram node references the actual source file path"
    - "The extension host / webview boundary and postMessage bridge are explicitly documented"
    - "The render pipeline draw order is documented with diagram"
    - "The agent event data flow from JSONL to canvas is documented with diagram"
    - "The asset pipeline from cortex-assets to canvas draw calls is documented with diagram"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Complete architecture documentation with Mermaid diagrams"
      contains: "graph TD"
  key_links:
    - from: "ARCHITECTURE.md diagrams"
      to: "actual import statements in src/"
      via: "manual verification"
      pattern: "Every diagram edge matches a real import"
---

<objective>
Create a top-level ARCHITECTURE.md with Mermaid diagrams documenting the current codebase state for human review. This is the "lite" documentation phase — enough for a developer to understand the system in 10 minutes.

Purpose: Phase 19 (Architecture Refactor) depends on this for planning. Accurate documentation of the as-is state is critical.
Output: Single ARCHITECTURE.md at repository root with 5-6 focused Mermaid diagrams and annotated prose.
</objective>

<execution_context>
@/Users/antonmoroz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonmoroz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-architecture-documentation-lite-code-linked-diagrams-of-current-codebase-state-for-human-review/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ARCHITECTURE.md with all diagrams and module index</name>
  <files>ARCHITECTURE.md</files>
  <action>
Create a top-level `ARCHITECTURE.md` with 6 sections, each containing a Mermaid diagram and annotated prose. The documentation must reflect the ACTUAL codebase — verify every diagram edge against real `import` statements by reading source files.

**Section 1: System Overview**
- Mermaid diagram showing the two runtime contexts: Extension Host (Node.js) and Webview (Browser)
- Show the postMessage bridge between extension.ts and webview.tsx
- Show VS Code API surface (workspace, webview panel, file system)
- Show external integrations (JSONL files, GitHub Projects via gh CLI, cortex-assets)

**Section 2: Module Dependency Graph**
- Full import graph with all 32 source files grouped into subgraphs by architectural layer:
  - Extension Host: extension.ts, agentManager.ts, fileWatcher.ts, transcriptParser.ts, githubProjects.ts
  - Webview React: webview.tsx, RoomCanvas.tsx, AvatarBuilderModal.tsx, LayoutEditorPanel.tsx, AvatarDebugGrid.tsx
  - Renderers: isoTileRenderer.ts, isoWallRenderer.ts, isoFurnitureRenderer.ts, isoAvatarRenderer.ts, isoBubbleRenderer.ts, isoNameTagRenderer.ts, isoKanbanRenderer.ts
  - Game Logic: avatarManager.ts, isoAgentBehavior.ts, isoPathfinding.ts, isoLayoutEditor.ts, idleWander.ts, avatarSelection.ts
  - Infrastructure: isometricMath.ts, isoTypes.ts, isoSpriteCache.ts, isoAudioManager.ts, furnitureRegistry.ts, avatarOutfitConfig.ts, agentTypes.ts, avatarBuilderPreview.ts
- Every edge must correspond to an actual `import` statement (read source files to verify)
- Use `graph TD` (top-down) for readability

**Section 3: Render Pipeline**
- Mermaid diagram showing the rAF loop draw order in RoomCanvas.tsx:
  1. Clear canvas
  2. Blit OffscreenCanvas (pre-rendered floor + walls)
  3. Depth-sort renderables (tiles, furniture seat parts, avatars, furniture backrest parts)
  4. Draw depth-sorted renderables
  5. Draw overlays (speech bubbles, name tags, kanban notes)
  6. Draw hover highlight (layout editor)
- Show the Renderable abstraction from isoTypes.ts and how depthSort works
- Note the OffscreenCanvas pre-render strategy (only rebuilt on layout change)

**Section 4: Data Flow — Agent Events**
- Mermaid sequence diagram or flowchart showing:
  JSONL file → fileWatcher.ts → transcriptParser.ts → agentManager.ts → postMessage → RoomCanvas.tsx → avatarManager.ts → avatar renderer
- Document the AgentState and AgentEvent types from agentTypes.ts
- Show state machine transitions: spawn → idle → walking → typing → waiting → idle

**Section 5: Asset Pipeline**
- Mermaid diagram showing:
  CakeChloe/cortex-assets (GitHub) → download-habbo-assets.mjs → cortex JSON → convert-cortex-to-nitro.mjs → Nitro JSON + PNGs → esbuild copy → dist/webview-assets/ → webview.asWebviewUri() → isoSpriteCache.ts → renderers
- Document the Nitro frame key convention (e.g., h_std_bd_1_0_0)
- Note furniture vs figure offset conventions (furniture: negated; figure: direct from offset string)

**Section 6: Module Index**
- Table with columns: File | Purpose | Lines | Key Exports | Layer
- Cover all 32 source files
- Use line counts from research (or verify with wc -l)
- Group by layer (Extension Host, Webview React, Renderers, Game Logic, Infrastructure)

**Important guidelines:**
- Document what IS, not what SHOULD BE (Phase 19 handles refactoring)
- ~60% diagrams, ~40% prose
- Every diagram node must include the actual file name
- Include a "Verified against" date at the bottom
- Do NOT include scripts/ or tests/ in the module dependency graph — focus on src/ runtime code only
- Do NOT create overly detailed function-level diagrams — keep at module level
  </action>
  <verify>
    <automated>test -f ARCHITECTURE.md && grep -c "mermaid" ARCHITECTURE.md | grep -v "^0$" && echo "PASS: ARCHITECTURE.md exists with Mermaid diagrams"</automated>
  </verify>
  <done>ARCHITECTURE.md exists at repository root with 6 sections, each containing a Mermaid diagram. All diagram edges correspond to actual import statements in the source code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of architecture documentation</name>
  <files>ARCHITECTURE.md</files>
  <action>
    Human reviews ARCHITECTURE.md for accuracy and completeness:
    1. Open ARCHITECTURE.md in VS Code (Mermaid diagrams render in preview)
    2. Check Section 1 (System Overview): Does the extension host / webview split look correct?
    3. Check Section 2 (Module Graph): Do the import edges match your understanding of the codebase?
    4. Check Section 3 (Render Pipeline): Does the draw order match the actual rAF loop?
    5. Check Section 4 (Data Flow): Does the JSONL to avatar path look correct?
    6. Check Section 5 (Asset Pipeline): Does the cortex to Nitro to SpriteCache flow look right?
    7. Check Section 6 (Module Index): Are all 32 files listed with correct purposes?
    8. Verify you can orient yourself in the codebase within 10 minutes using only this document.
  </action>
  <verify>
    <automated>echo "Human review checkpoint — manual approval required"</automated>
  </verify>
  <done>Human reviewer confirms architecture documentation is accurate and complete. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
- ARCHITECTURE.md exists at repository root
- Contains 6 sections with Mermaid diagrams
- All diagram edges verified against actual import statements
- Module index covers all 32 source files
- No aspirational/refactored architecture — documents actual state only
- `npx vitest run` still passes (no code changes)
</verification>

<success_criteria>
- A developer unfamiliar with the codebase can understand the architecture within 10 minutes
- Every diagram node references a real source file
- The extension host / webview boundary is explicitly documented
- Human reviewer approves the documentation as accurate
</success_criteria>

<output>
After completion, create `.planning/phases/18-architecture-documentation-lite-code-linked-diagrams-of-current-codebase-state-for-human-review/18-01-SUMMARY.md`
</output>

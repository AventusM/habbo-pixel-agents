---
phase: 16-agent-factory-workflow
plan: 07
type: execute
wave: 3
depends_on:
  - 16-05
files_modified:
  - src/avatarOutfitConfig.ts
  - src/idleWander.ts
  - src/RoomCanvas.tsx
autonomous: true
requirements:
  - AF-18
  - AF-19

must_haves:
  truths:
    - "Each team role has a distinct default outfit preset"
    - "Agents assigned to a team get the correct role-based outfit on spawn"
    - "Coders sit at desks when idle, Planners pace near conference tables, Researchers browse bookshelves"
  artifacts:
    - path: "src/avatarOutfitConfig.ts"
      provides: "Role-based outfit presets"
      contains: "ROLE_OUTFIT_PRESETS"
    - path: "src/idleWander.ts"
      provides: "Role-specific idle behaviors"
      contains: "RoleIdleBehavior"
  key_links:
    - from: "src/avatarOutfitConfig.ts"
      to: "src/agentClassifier.ts"
      via: "TeamSection type for role outfit mapping"
      pattern: "TeamSection"
    - from: "src/idleWander.ts"
      to: "src/sectionManager.ts"
      via: "section tile lookup for idle targets"
      pattern: "getIdleTile\\|getDeskTile"
---

<objective>
Add role-based outfit presets and role-specific idle behaviors so agents visually match their team and behave appropriately when idle.

Purpose: Visual and behavioral differentiation makes agents recognizable. Planners look and act different from coders.
Output: Role outfit presets in avatarOutfitConfig, role-specific idle behavior in IdleWanderManager.
</objective>

<execution_context>
@/Users/antonmoroz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonmoroz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-CONTEXT.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-05-SUMMARY.md

<interfaces>
From src/avatarOutfitConfig.ts:
```typescript
export interface OutfitConfig {
  gender: 'M' | 'F';
  skinColor: string;
  parts: OutfitPart[];
}
export interface OutfitPart {
  type: PartType;
  setId: string;
  colorIndex: number;
}
export const DEFAULT_PRESETS: OutfitConfig[];
export function getDefaultPreset(variant: number): OutfitConfig;
```

From src/idleWander.ts:
```typescript
export class IdleWanderManager {
  startWandering(agentId: string): void;
  stopWandering(agentId: string): void;
  tick(currentMs: number, avatarManager: AvatarManager, grid: TileGrid, blocked: Set<string>, ...): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create role-based outfit presets</name>
  <files>src/avatarOutfitConfig.ts</files>
  <action>
    1. Add `ROLE_OUTFIT_PRESETS: Record<TeamSection, OutfitConfig>` to `avatarOutfitConfig.ts`:
       - Planning: Formal/business look — cardigan or blazer top, darker pants, professional colors (navy, charcoal)
       - Core Dev: Casual/tech look — t-shirt or hoodie, jeans, brighter colors (green, blue)
       - Infrastructure: Utility/ops look — vest or work shirt, cargo-style pants, earth tones (brown, olive)
       - Support: Clean/diagnostic look — polo or collared shirt, neat pants, neutral colors (gray, white)

       Use existing clothing setIds from the curated catalog in avatarOutfitConfig.ts. Pick distinct combinations that are visually distinguishable at the small Habbo sprite scale. Each preset should use different `colorIndex` values to further differentiate.

    2. Add `getRolePreset(team: TeamSection, variant: number): OutfitConfig`:
       - Returns the ROLE_OUTFIT_PRESETS[team] as base, but applies variant-based skin color variation (cycle through existing skin colors).
       - This is used when an agent is first classified — if they don't have a saved outfit, they get the role preset.

    3. Add `ROLE_ACCESSORY_PRESETS: Record<TeamSection, OutfitPart[]>` (optional additions on top of outfit):
       - Planning: No extra (outfit is sufficient)
       - Core Dev: No extra
       - Infrastructure: No extra
       - Support: No extra
       Note: Accessories are a stretch goal. Define the structure but don't block on it — the base outfit difference is what matters for visual identity.

    4. Export `getRolePreset` for use in avatarManager when spawning classified agents.
  </action>
  <verify>npx vitest run --reporter=verbose</verify>
  <done>ROLE_OUTFIT_PRESETS defined for all 4 teams, getRolePreset returns variant-adjusted outfit</done>
</task>

<task type="auto">
  <name>Task 2: Implement role-specific idle behaviors</name>
  <files>src/idleWander.ts, src/RoomCanvas.tsx</files>
  <action>
    1. Update `src/idleWander.ts` to support role-specific idle behavior:
       - Add `RoleIdleBehavior` type: `'wander' | 'sit-at-desk' | 'pace' | 'browse'`
       - Add `setAgentRole(agentId: string, team: TeamSection): void` — stores the role for idle behavior selection
       - Modify `tick()` idle target selection:
         - Core Dev ('sit-at-desk'): When idle timer fires, find nearest unoccupied desk chair in agent's section and walk to sit. If all occupied, wander normally within section.
         - Planning ('pace'): Walk between conference table and whiteboard positions in a cycle. Longer pause at each destination (8-12s instead of default 5-8s).
         - Infrastructure/Support ('wander'): Default behavior — random walkable tile within section bounds.
       - Add method `getIdleTargetForRole(agentId: string, avatarManager: AvatarManager, sectionManager: SectionManager, grid: TileGrid, blocked: Set<string>): {x,y} | null`

    2. Update `src/RoomCanvas.tsx`:
       - In the `agentCreated` handler, after spawning and section assignment, call `idleWander.setAgentRole(agentId, team)` so idle behavior matches team.
       - Pass `sectionManagerRef.current` to `idleWanderRef.current.tick()` — this requires updating the tick method signature to accept an optional SectionManager parameter.
       - Fallback: if no sectionManager, use default random wander (backward compatible).
  </action>
  <verify>npx vitest run --reporter=verbose</verify>
  <done>Idle agents exhibit team-appropriate behaviors: coders sit at desks, planners pace between table/whiteboard, others wander within section</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
</verification>

<success_criteria>
- Each team has a visually distinct outfit preset
- New agents get role-appropriate outfits on spawn
- Coders preferentially sit at desks when idle
- Planners pace between furniture in their section
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-07-SUMMARY.md`
</output>

---
phase: 16-agent-factory-workflow
plan: 09
type: execute
wave: 4
depends_on:
  - 16-06
  - 16-07
  - 16-08
files_modified:
  - src/RoomCanvas.tsx
  - src/isoFurnitureRenderer.ts
  - src/AvatarBuilderModal.tsx
autonomous: false
requirements:
  - AF-25
  - AF-26
  - AF-27
  - AF-28

must_haves:
  truths:
    - "Clicking an agent shows a popup card near them with role and section info"
    - "Monitors glow when agents at desks are active"
    - "Lamps toggle on/off based on section activity"
    - "Avatar Builder has a 'Role Outfits' tab for customizing role-to-outfit mapping"
    - "Camera auto-follow toggle tracks section with most activity"
  artifacts:
    - path: "src/RoomCanvas.tsx"
      provides: "Agent popup cards, auto-follow camera, interactive furniture"
      contains: "drawAgentPopup"
    - path: "src/isoFurnitureRenderer.ts"
      provides: "Furniture state rendering (active/inactive)"
      contains: "furnitureState"
    - path: "src/AvatarBuilderModal.tsx"
      provides: "Role Outfits tab in builder"
      contains: "Role Outfits"
  key_links:
    - from: "src/RoomCanvas.tsx"
      to: "src/sectionManager.ts"
      via: "section activity for auto-follow"
      pattern: "getMostActiveSection"
    - from: "src/isoFurnitureRenderer.ts"
      to: "src/sectionManager.ts"
      via: "activity level for furniture state"
      pattern: "activityLevel"
---

<objective>
Final integration: agent popup cards, activity-linked furniture states, Avatar Builder role outfits tab, camera auto-follow, and visual polish checkpoint.

Purpose: Completes the Phase 16 feature set with interactive furniture, in-room agent info, and camera automation. Human verification ensures everything looks right.
Output: Full Phase 16 feature set integrated and visually verified.
</objective>

<execution_context>
@/Users/antonmoroz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonmoroz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-CONTEXT.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-06-SUMMARY.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-07-SUMMARY.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent popup cards and activity-linked furniture</name>
  <files>src/RoomCanvas.tsx, src/isoFurnitureRenderer.ts</files>
  <action>
    1. **Agent popup cards** in `src/RoomCanvas.tsx`:
       - Add `popupAgentRef = useRef<string | null>(null)` for tracking which agent has an open popup
       - In `handleClick`, when clicking an avatar: set `popupAgentRef.current = clickedAvatar.id` (in addition to opening builder). Also send `{ type: 'agentClicked', agentId }` to extension via vscodeApi for sidebar scroll-to.
       - Add `drawAgentPopup(ctx, avatar, cameraOrigin)` function: draws a small card near the avatar (above name tag) showing:
         - Role and task area (e.g., "Executor - Frontend")
         - Team section name
         - Current status (active/idle)
         - Small "x" to dismiss
       - Style: dark rounded rect with Habbo pixel font, team-colored header bar
       - Popup dismisses on click outside or after 5 seconds

    2. **Activity-linked furniture states** in `src/isoFurnitureRenderer.ts`:
       - Add `FurnitureState` interface: `{ furnitureId: string; tileX: number; tileY: number; isActive: boolean }`
       - Add `drawFurnitureActiveOverlay(ctx, spec, screenX, screenY, isActive)`:
         - For `tv_flat` (monitors): when active, draw a subtle blue-white glow rectangle overlay on the screen area using `globalCompositeOperation = 'lighter'` with low alpha (0.15)
         - For `hc_lmp` (lamps): when active, draw a small warm yellow radial gradient glow around the lamp base
         - For other furniture: no overlay
       - This overlay is drawn AFTER the furniture sprite in the render loop, not replacing it.

    3. **Auto-follow camera** in `src/RoomCanvas.tsx`:
       - Add `autoFollowRef = useRef(false)` and expose toggle via a button or extensionMessage
       - In `frame()`, if autoFollow is on:
         - Every 3 seconds, check `sectionManager.getMostActiveSection()` (section with most recent agentTool events)
         - If different from current viewed section, smoothly pan camera to center on that section using `jumpToSection`
         - Add `lerp` for smooth camera transition (pan 10% per frame toward target)
       - Handle `jumpToSection` message from sidebar: compute section center in world coords, call `jumpToSection` on cameraState

    4. Update `sectionManager.ts` (if needed): add `getMostActiveSection(): TeamSection` — returns team with highest `lastActivityMs`. Add `updateActivity(team: TeamSection, currentMs: number): void` — called when agentTool events arrive.
  </action>
  <verify>npx vitest run --reporter=verbose</verify>
  <done>Agent popup cards show on click, monitors glow when agents active, lamps reflect section activity, auto-follow camera tracks active section</done>
</task>

<task type="auto">
  <name>Task 2: Avatar Builder role outfits tab</name>
  <files>src/AvatarBuilderModal.tsx</files>
  <action>
    1. Add a "Role Outfits" tab to the Avatar Builder panel:
       - New tab button alongside existing clothing/color tabs
       - Tab content shows 4 role preset cards: Planning, Core Dev, Infrastructure, Support
       - Each card shows the team name, a small preview of the outfit (text description or colored swatch), and an "Apply" button
       - Clicking "Apply" loads the `ROLE_OUTFIT_PRESETS[team]` into the builder's current outfit state
       - User can then customize further before saving

    2. The Role Outfits tab is informational and functional:
       - Shows which team the current avatar belongs to (highlighted)
       - Allows applying any team's outfit regardless of assignment (for customization)
       - "Reset to Role Default" button that reloads the preset for the avatar's assigned team

    3. Keep the existing clothing/color tabs working as before — Role Outfits is an additional tab.
  </action>
  <verify>npx vitest run --reporter=verbose</verify>
  <done>Avatar Builder has Role Outfits tab with 4 team presets, apply/reset functionality works</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 16</name>
  <files>src/RoomCanvas.tsx</files>
  <action>Human verifies the complete Phase 16 visual and functional experience by launching the extension and testing all features end-to-end.</action>
  <verify>Manual visual inspection in Extension Development Host</verify>
  <done>Human approves: 4-section floor, camera pan/zoom, sidebar panel, teleport spawning, role outfits, popup cards, furniture states all working correctly</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
Human verification of complete Phase 16 visual and functional experience
</verification>

<success_criteria>
- Agent popup cards show role/team info on click
- Monitor furniture glows when agents at desks are active
- Avatar Builder has Role Outfits tab
- Auto-follow camera tracks most active section
- Human approves overall visual quality
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-09-SUMMARY.md`
</output>

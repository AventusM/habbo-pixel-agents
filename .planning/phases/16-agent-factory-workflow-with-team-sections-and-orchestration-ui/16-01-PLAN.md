---
phase: 16-agent-factory-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentClassifier.ts
  - src/agentTypes.ts
  - tests/agentClassifier.test.ts
autonomous: true
requirements:
  - AF-01
  - AF-02
  - AF-03

must_haves:
  truths:
    - "Agents with subagent_type in JSONL are classified into correct role and team"
    - "Agents without subagent_type trigger a classification request (popup signal)"
    - "Agent display names follow '<Role> - <Task>' format"
    - "Role-to-team mapping covers all GSD subagent types"
  artifacts:
    - path: "src/agentClassifier.ts"
      provides: "Agent classification pipeline"
      exports: ["classifyAgent", "inferTaskArea", "ROLE_TO_TEAM", "TeamSection", "AgentClassification"]
      min_lines: 80
    - path: "src/agentTypes.ts"
      provides: "Extended agent types with role/team/taskArea fields"
      contains: "role"
    - path: "tests/agentClassifier.test.ts"
      provides: "Classification logic tests"
      min_lines: 60
  key_links:
    - from: "src/agentClassifier.ts"
      to: "src/agentTypes.ts"
      via: "type imports"
      pattern: "import.*AgentClassification"
---

<objective>
Build the agent classification system that maps JSONL transcript data to roles, teams, and display names.

Purpose: Foundation for all role-based features (outfits, section assignment, idle behaviors, sidebar grouping). Without classification, agents have no identity beyond "Claude N".
Output: `agentClassifier.ts` with classification pipeline, extended `agentTypes.ts`, and comprehensive tests.
</objective>

<execution_context>
@/Users/antonmoroz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonmoroz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-CONTEXT.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-RESEARCH.md

<interfaces>
From src/agentTypes.ts:
```typescript
export interface AgentState {
  agentId: string;
  terminalName: string;
  variant: 0 | 1 | 2 | 3 | 4 | 5;
  status: AgentStatus;
  lastActivityMs: number;
  jsonlPath: string;
}

export type ExtensionMessage =
  | { type: 'agentCreated'; agentId: string; terminalName: string; variant: 0 | 1 | 2 | 3 | 4 | 5 }
  | { type: 'agentRemoved'; agentId: string }
  | { type: 'agentStatus'; agentId: string; status: AgentStatus }
  | { type: 'agentTool'; agentId: string; toolName: string; displayText: string }
  // ... other message types
```

From src/transcriptParser.ts:
```typescript
export function parseTranscriptLine(line: string): AgentEvent | null;
// Already parses tool_use blocks but does NOT extract subagent_type from Agent tool input
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent classification types and logic</name>
  <files>src/agentClassifier.ts, src/agentTypes.ts</files>
  <action>
    1. Extend `AgentState` in `agentTypes.ts` with optional fields: `role?: string`, `team?: TeamSection`, `taskArea?: string`, `displayName` (already exists on AvatarSpec but not AgentState). Add `TeamSection` type: `'planning' | 'core-dev' | 'infrastructure' | 'support'`. Add new `ExtensionMessage` variant: `{ type: 'classifyAgent'; agentId: string; jsonlPath: string }` for requesting user popup when no subagent_type is found. Add `WebviewMessage` variant for reassignment: `{ type: 'reassignAgent'; agentId: string; team: TeamSection }`.

    2. Create `src/agentClassifier.ts` with:
       - `ROLE_TO_TEAM` map: `gsd-phase-researcher` -> planning, `gsd-planner` -> planning, `gsd-plan-checker` -> planning, `gsd-executor` -> core-dev, `gsd-codebase-mapper` -> infrastructure, `gsd-debugger` -> support, `gsd-verifier` -> support. Default unmapped roles to 'core-dev'.
       - `ROLE_DISPLAY_NAMES` map: `gsd-executor` -> 'Executor', `gsd-planner` -> 'Planner', `gsd-phase-researcher` -> 'Researcher', `gsd-plan-checker` -> 'Checker', `gsd-codebase-mapper` -> 'Mapper', `gsd-debugger` -> 'Debugger', `gsd-verifier` -> 'Verifier'. Unknown -> capitalize first word after last hyphen.
       - `extractSubagentType(jsonlContent: string): string | null` — scan JSONL lines for `tool_use` with `name: 'Agent'` and extract `input.subagent_type`. Return first match found.
       - `inferTaskArea(jsonlContent: string): string` — scan file paths from Read/Write/Edit tool uses, categorize: `src/components/` or `.tsx` -> 'Frontend', `src/api/` or `route.ts` -> 'Backend', `tests/` -> 'Testing', `prisma/` or `schema` -> 'Database', `.planning/` -> 'Planning', fallback to 'General'.
       - `classifyAgent(subagentType: string | null, jsonlContent: string): AgentClassification` — combines role lookup, team assignment, task area inference, and display name generation in `<Role> - <Task>` format.
       - Export `AgentClassification` interface: `{ role: string; roleName: string; team: TeamSection; taskArea: string; displayName: string }`.
  </action>
  <verify>npx vitest run tests/agentClassifier.test.ts -x</verify>
  <done>Classification functions exported, ROLE_TO_TEAM covers all GSD types, display name format is '<Role> - <Task>'</done>
</task>

<task type="auto">
  <name>Task 2: Write classification tests</name>
  <files>tests/agentClassifier.test.ts</files>
  <action>
    Create `tests/agentClassifier.test.ts` with test cases:

    1. `extractSubagentType` tests:
       - Returns 'gsd-executor' from JSONL with Agent tool_use containing subagent_type
       - Returns null from JSONL without any Agent tool_use
       - Returns null from empty/invalid JSONL
       - Handles multiple tool_use blocks (returns first Agent match)

    2. `classifyAgent` tests:
       - 'gsd-executor' maps to team 'core-dev', roleName 'Executor'
       - 'gsd-planner' maps to team 'planning', roleName 'Planner'
       - 'gsd-debugger' maps to team 'support', roleName 'Debugger'
       - 'gsd-codebase-mapper' maps to team 'infrastructure', roleName 'Mapper'
       - Unknown type defaults to 'core-dev'
       - null subagentType returns team 'core-dev' with roleName derived from task area

    3. `inferTaskArea` tests:
       - JSONL with Read of `.tsx` files -> 'Frontend'
       - JSONL with Write of `tests/` files -> 'Testing'
       - JSONL with Edit of `.planning/` files -> 'Planning'
       - Empty JSONL -> 'General'

    4. Display name format:
       - classifyAgent('gsd-executor', jsonlWithFrontendFiles) -> displayName 'Executor - Frontend'
       - classifyAgent(null, jsonlWithTestFiles) -> displayName 'Agent - Testing'

    Use realistic JSONL line format: `{"role":"assistant","content":[{"type":"tool_use","name":"Agent","input":{"subagent_type":"gsd-executor","prompt":"..."}}]}`
  </action>
  <verify>npx vitest run tests/agentClassifier.test.ts -x</verify>
  <done>All classification tests pass, covering subagent_type extraction, role mapping, task area inference, and display name format</done>
</task>

</tasks>

<verification>
npx vitest run tests/agentClassifier.test.ts -x
</verification>

<success_criteria>
- agentClassifier.ts exports classifyAgent, extractSubagentType, inferTaskArea, ROLE_TO_TEAM
- All GSD subagent types mapped to correct teams per CONTEXT.md
- Display names follow '<Role> - <Task>' format
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-01-SUMMARY.md`
</output>

---
phase: 16-agent-factory-workflow
plan: 04
type: execute
wave: 2
depends_on:
  - 16-02
files_modified:
  - src/furnitureRegistry.ts
  - src/teleportEffect.ts
  - src/roomLayoutEngine.ts
  - scripts/download-habbo-assets.mjs
  - tests/teleportEffect.test.ts
autonomous: true
requirements:
  - AF-09
  - AF-10
  - AF-11

must_haves:
  truths:
    - "Section-themed furniture items are registered in the catalog"
    - "Teleport booth (country_gate) renders at section spawn points"
    - "Teleport flash effect animates on spawn/despawn"
    - "New furniture assets are downloaded from cortex-assets"
  artifacts:
    - path: "src/furnitureRegistry.ts"
      provides: "Extended catalog with section-themed items"
      contains: "section"
    - path: "src/teleportEffect.ts"
      provides: "Teleport flash/glow Canvas 2D effect"
      exports: ["TeleportEffect", "drawTeleportFlash", "createTeleportEffect"]
      min_lines: 40
    - path: "tests/teleportEffect.test.ts"
      provides: "Teleport effect tests"
      min_lines: 30
  key_links:
    - from: "src/roomLayoutEngine.ts"
      to: "src/furnitureRegistry.ts"
      via: "furniture specs per section"
      pattern: "FurnitureSpec"
---

<objective>
Expand the furniture catalog with section-themed items and implement the teleport booth spawn/despawn effect.

Purpose: Sections need distinct themed furniture to feel like team zones. The teleport booth is the spawn/despawn point per user decision.
Output: Extended furniture catalog, teleport flash effect, new cortex-asset downloads, section furniture placement in layout templates.
</objective>

<execution_context>
@/Users/antonmoroz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonmoroz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-CONTEXT.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-RESEARCH.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-02-SUMMARY.md

<interfaces>
From src/furnitureRegistry.ts:
```typescript
export type FurnitureCategory = 'habboclub' | 'fun';
export interface FurnitureEntry {
  id: string;
  displayName: string;
  category: FurnitureCategory;
}
export const FURNITURE_CATALOG: FurnitureEntry[];
```

From src/roomLayoutEngine.ts (created in Plan 02):
```typescript
export interface SectionLayout {
  team: TeamSection;
  originTile: { x: number; y: number };
  widthTiles: number;
  heightTiles: number;
  teleportTile: { x: number; y: number };
  furniture: FurnitureSpec[];
  deskTiles: { x: number; y: number; dir: number }[];
  idleTiles: { x: number; y: number }[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand furniture catalog and download new assets</name>
  <files>src/furnitureRegistry.ts, scripts/download-habbo-assets.mjs</files>
  <action>
    1. Add new `FurnitureCategory` values: `'office'` and `'section'` to the union type.

    2. Add section-themed furniture to `FURNITURE_CATALOG`:
       - Planning: `country_gate` (teleport booth stand-in), `hc_bkshlf` (bookshelf, already exists), `hc_tbl` (conference table, already exists)
       - Core Dev: `hc_dsk` (workstation, already exists), `tv_flat` (monitor), `hc_lmp` (lamp, already exists)
       - Infrastructure: `shelves_armas` (server rack stand-in), `hc_lmp` (status lamp)
       - Support: `hc_bkshlf` (reference shelf), `hc_chr` (diagnostic station chair)
       - New items to download: `country_gate`, `tv_flat`, `shelves_armas`

    3. Update `scripts/download-habbo-assets.mjs` to include the new furniture asset IDs in the download list. These are cortex-assets items — verify they exist by checking the download script's existing pattern.

    4. Add `CATEGORY_LABELS` entries for the new categories: `office: 'Office'`, `section: 'Section Furniture'`.

    5. Add `country_gate` to a new `TELEPORT_IDS` set and export `isTeleportBooth(furnitureId: string): boolean`.
  </action>
  <verify>npx vitest run --reporter=verbose</verify>
  <done>Furniture catalog extended with section-themed items, new assets added to download script, teleport booth ID recognized</done>
</task>

<task type="auto">
  <name>Task 2: Create teleport flash effect and section furniture placement</name>
  <files>src/teleportEffect.ts, src/roomLayoutEngine.ts, tests/teleportEffect.test.ts</files>
  <action>
    1. Create `src/teleportEffect.ts`:
       - `TeleportEffect` interface: `{ screenX: number; screenY: number; startMs: number; durationMs: number; type: 'spawn' | 'despawn' }`
       - `createTeleportEffect(screenX: number, screenY: number, type: 'spawn'|'despawn'): TeleportEffect` — creates effect with 600ms duration
       - `drawTeleportFlash(ctx: CanvasRenderingContext2D, effect: TeleportEffect, currentMs: number): boolean` — draws Canvas 2D radial gradient centered at screenX/screenY. Alpha peaks at progress=0.5 using `Math.sin(progress * Math.PI)`. Radius grows from 20 to 60 pixels. Colors: white center, light blue outer ring. Uses `globalCompositeOperation = 'lighter'` for glow. Returns false when effect is complete (progress >= 1.0).
       - `isEffectActive(effect: TeleportEffect, currentMs: number): boolean`

    2. Update `src/roomLayoutEngine.ts` — add `getSectionFurniture(team: TeamSection, section: SectionLayout): FurnitureSpec[]`:
       - Planning: country_gate at teleportTile, hc_tbl at center area, hc_bkshlf along one wall, chairs around table
       - Core Dev: country_gate at teleportTile, hc_dsk at desk positions, tv_flat on desks, hc_lmp at corners
       - Infrastructure: country_gate at teleportTile, shelves_armas along walls, hc_lmp at corners
       - Support: country_gate at teleportTile, hc_bkshlf along walls, hc_chr at desk positions
       Update `generateFloorTemplate` to call `getSectionFurniture` and populate each section's `furniture` array.

    3. Create `tests/teleportEffect.test.ts`:
       - createTeleportEffect sets correct defaults (600ms duration)
       - drawTeleportFlash returns true while active (progress < 1.0)
       - drawTeleportFlash returns false when complete (progress >= 1.0)
       - isEffectActive returns false after duration elapsed
       - Effect at progress=0.5 has peak alpha (test Math.sin calculation)
  </action>
  <verify>npx vitest run tests/teleportEffect.test.ts -x && npx vitest run tests/roomLayoutEngine.test.ts -x</verify>
  <done>Teleport flash effect draws correctly, sections have themed furniture, all tests pass</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
</verification>

<success_criteria>
- Furniture catalog includes section-themed items with new categories
- country_gate registered as teleport booth
- Teleport flash effect renders radial gradient glow
- Each section template includes appropriate themed furniture
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-04-SUMMARY.md`
</output>

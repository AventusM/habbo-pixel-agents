---
phase: 16-agent-factory-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/roomLayoutEngine.ts
  - tests/roomLayoutEngine.test.ts
autonomous: true
requirements:
  - AF-04
  - AF-05
  - AF-06

must_haves:
  truths:
    - "Three template sizes (small/medium/large) produce valid heightmaps"
    - "Each template has 4 sections in a 2x2 grid with open doorways"
    - "Section boundaries include teleport booth tile positions"
    - "Generated heightmaps are all flat elevation (height 0)"
  artifacts:
    - path: "src/roomLayoutEngine.ts"
      provides: "Room layout template engine"
      exports: ["generateFloorTemplate", "FloorTemplate", "SectionLayout", "TEMPLATE_SIZES"]
      min_lines: 120
    - path: "tests/roomLayoutEngine.test.ts"
      provides: "Layout generation tests"
      min_lines: 60
  key_links:
    - from: "src/roomLayoutEngine.ts"
      to: "src/isoTypes.ts"
      via: "heightmap string format"
      pattern: "generateFloorTemplate"
---

<objective>
Build the room layout engine that generates 2x2 section grid templates with visual dividers and doorways.

Purpose: The multi-section floor is the spatial foundation for team zones. Without it, agents have no sections to be assigned to.
Output: `roomLayoutEngine.ts` with template generation for 3 sizes, and tests.
</objective>

<execution_context>
@/Users/antonmoroz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonmoroz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-CONTEXT.md
@.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-RESEARCH.md

<interfaces>
From src/isoTypes.ts:
```typescript
export function parseHeightmap(text: string): TileGrid;
// Heightmap is a string with rows separated by '\n'
// '0'-'9' = walkable tiles at that height, 'x' = void
```

From src/isoFurnitureRenderer.ts:
```typescript
export interface FurnitureSpec {
  name: string;
  tileX: number;
  tileY: number;
  direction: number;
}
export interface MultiTileFurnitureSpec extends FurnitureSpec {
  widthTiles: number;
  heightTiles: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create room layout engine with 3 template sizes</name>
  <files>src/roomLayoutEngine.ts</files>
  <action>
    Create `src/roomLayoutEngine.ts` with:

    1. Types:
       - `TeamSection = 'planning' | 'core-dev' | 'infrastructure' | 'support'` (import from agentTypes or redefine locally to avoid circular deps — prefer local redefine with re-export)
       - `SectionLayout`: `{ team: TeamSection; originTile: {x,y}; widthTiles: number; heightTiles: number; teleportTile: {x,y}; deskTiles: {x,y,dir}[]; idleTiles: {x,y}[] }`
       - `FloorTemplate`: `{ size: 'small'|'medium'|'large'; totalWidth: number; totalHeight: number; heightmap: string; sections: SectionLayout[]; dividerTiles: {x,y}[]; doorwayTiles: {x,y}[] }`

    2. `TEMPLATE_SIZES` constant:
       - Small: 20x20 total (each section ~8x8 usable, 1-tile border, 2-tile divider strip)
       - Medium: 28x28 total (each section ~12x12 usable)
       - Large: 36x36 total (each section ~16x16 usable)

    3. `generateFloorTemplate(size: 'small'|'medium'|'large'): FloorTemplate`:
       - Generate heightmap string with all '0' tiles for walkable areas, 'x' for void border and divider walls
       - 2x2 grid layout: Planning (top-left), Core Dev (top-right), Infrastructure (bottom-left), Support (bottom-right)
       - Dividers between sections are 'x' tiles (void) acting as low wall partitions
       - Each divider has 2-tile-wide doorway openings (set as '0') at the midpoint
       - Each section gets a teleport booth tile (corner of the section, 1 tile from the divider)
       - Each section gets desk tiles (interior, away from dividers) and idle/wander tiles
       - All tiles at height 0 (flat elevation per user decision)

    4. Helper: `getSectionForTeam(template: FloorTemplate, team: TeamSection): SectionLayout | undefined`
    5. Helper: `getTemplateSize(agentCount: number): 'small'|'medium'|'large'` — smart default: <=12 agents small, <=24 medium, else large

    Divider pattern: For a 20x20 small template, the center row (y=9) and center column (x=9) are 'x' tiles except at doorway positions. Doorway positions are at the midpoint of each section edge.
  </action>
  <verify>npx vitest run tests/roomLayoutEngine.test.ts -x</verify>
  <done>generateFloorTemplate produces valid heightmaps for all 3 sizes with 4 sections, doorways, and teleport tiles</done>
</task>

<task type="auto">
  <name>Task 2: Write layout engine tests</name>
  <files>tests/roomLayoutEngine.test.ts</files>
  <action>
    Create `tests/roomLayoutEngine.test.ts` with test cases:

    1. Template generation:
       - Small template produces a 20x20 heightmap (20 rows, each 20 chars)
       - Medium template produces 28x28 heightmap
       - Large template produces 36x36 heightmap
       - All templates have exactly 4 sections

    2. Section boundaries:
       - Each section's originTile is within the heightmap bounds
       - No two sections overlap (check originTile + width/height ranges)
       - Each section has at least 1 teleport tile
       - Each section has at least 2 desk tiles

    3. Dividers and doorways:
       - Divider tiles are 'x' (void) in the heightmap
       - Doorway tiles are '0' (walkable) in the heightmap
       - At least 1 doorway per divider line (horizontal and vertical)

    4. Heightmap validity:
       - parseHeightmap(template.heightmap) produces a valid TileGrid without throwing
       - All tiles are either '0' or 'x' (no heights > 0)
       - Border tiles are 'x' (void perimeter)

    5. Smart size selection:
       - getTemplateSize(8) returns 'small'
       - getTemplateSize(16) returns 'medium'
       - getTemplateSize(30) returns 'large'

    Import parseHeightmap from isoTypes.ts to validate heightmap output.
  </action>
  <verify>npx vitest run tests/roomLayoutEngine.test.ts -x</verify>
  <done>All layout engine tests pass, validating template sizes, section boundaries, dividers, and doorways</done>
</task>

</tasks>

<verification>
npx vitest run tests/roomLayoutEngine.test.ts -x
</verification>

<success_criteria>
- 3 template sizes generate valid heightmaps parseable by existing parseHeightmap
- 4 sections in 2x2 grid with no overlap
- Dividers with doorway openings between all adjacent sections
- Teleport tiles and desk tiles assigned per section
</success_criteria>

<output>
After completion, create `.planning/phases/16-agent-factory-workflow-with-team-sections-and-orchestration-ui/16-02-SUMMARY.md`
</output>

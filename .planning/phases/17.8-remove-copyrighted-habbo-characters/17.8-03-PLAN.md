---
phase: 17.8-remove-copyrighted-habbo-characters
plan: 03
type: execute
wave: 3
depends_on: ["17.8-02"]
files_modified:
  - src/isoSpriteCache.ts
autonomous: false
requirements:
  - REM-10

must_haves:
  truths:
    - "No remaining references to deleted Habbo figure files anywhere in the codebase"
    - "No dead imports or orphaned references to figure/outfit/builder code"
    - "All tests pass (pre-existing failures excluded)"
    - "Room view renders agents with correct team-specific PixelLab characters"
    - "Character legend panel is visible and correctly maps teams to characters"
  artifacts: []
  key_links: []
---

<objective>
Full codebase audit for leftover Habbo figure references and UAT verification.

Purpose: Per user requirements, a full codebase audit after removal is mandatory to catch any remaining figure-related imports, references, or dead code paths. This plan also includes a human verification checkpoint to confirm the room view renders correctly with team-specific characters and the legend panel.

Output: Clean codebase with zero Habbo figure/character references. Visual verification of room rendering.
</objective>

<execution_context>
@/Users/antonmoroz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonmoroz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17.8-remove-copyrighted-habbo-characters/17.8-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full codebase audit for remaining Habbo figure references</name>
  <files><!-- Read-only audit task: no output files. Scans src/, tests/, scripts/ for stale references and fixes any found in-place. Also audits src/isoSpriteCache.ts for figure-only fields/branches per user decision. --></files>
  <action>
Run a comprehensive grep/scan for any remaining references to Habbo figure/character code. Fix any issues found.

**Audit `src/isoSpriteCache.ts` for figure-only remnants:**
- Open `src/isoSpriteCache.ts` and review NitroAssetData type and all helper functions
- Remove any fields, branches, or code paths that only served the Habbo figure renderer (e.g., figure-specific offset parsing, body-part logic)
- Keep everything that furniture rendering still needs (NitroAssetData, loadNitroAsset, getNitroFrame, etc.)
- If no figure-only code exists, confirm with a comment in the summary

**Search patterns to check:**
1. `grep -r "isoAvatarRenderer" src/ tests/ scripts/` — should return zero results
2. `grep -r "avatarOutfitConfig" src/ tests/ scripts/` — should return zero results
3. `grep -r "AvatarBuilderModal\|AvatarBuilderPanel\|avatarBuilderPreview" src/ tests/` — should return zero results
4. `grep -r "AvatarDebugGrid" src/ tests/` — should return zero results
5. `grep -r "OutfitConfig" src/ tests/` — should return zero results
6. `grep -r "FIGURE_CATALOG\|FIGURE_ITEMS\|ROLE_OUTFIT_PRESETS\|ROLE_ACCESSORY" src/ tests/ scripts/` — should return zero results
7. `grep -r "nextBlinkMs\|blinkFrame" src/ tests/` — should return zero results (unless in comments explaining removal)
8. `grep -r "nitroFiguresBase\|nitro.*figures\|hh_human" src/ tests/ scripts/` — should return zero results
9. `grep -r "habboRenderer\|habbo.*renderer" src/` — should return zero results (case insensitive for habbo+renderer combination)
10. `grep -r "savedOutfits\|avatars\.json\|loadAvatars\|saveAvatar" src/` — should return zero results
11. `grep -r "cortex.*figure\|figure.*cortex" scripts/` — should return zero results
12. `grep -r "figure\|blink\|outfit" src/isoSpriteCache.ts` — audit for figure-only fields/branches; remove any found

**For each match found:** Determine if it's a dead reference, stale comment, or actual code path. Fix by removing the reference. If it's in a comment explaining history, that's acceptable — leave it.

**Also verify:**
- `npx tsc --noEmit` — clean compilation
- `npx vitest run` — all tests pass (pre-existing failures excluded)
- No orphaned type imports (TypeScript will catch most of these)

If any fixes are needed, make them and re-run the verification.
  </action>
  <verify>
    <automated>grep -r "isoAvatarRenderer\|avatarOutfitConfig\|AvatarBuilderModal\|avatarBuilderPreview\|AvatarDebugGrid\|OutfitConfig\|FIGURE_CATALOG\|ROLE_OUTFIT_PRESETS\|nextBlinkMs\|blinkFrame\|nitroFiguresBase\|habboRenderer\|savedOutfits" src/ tests/ scripts/ 2>/dev/null | grep -v "node_modules" | grep -v ".planning" | wc -l | xargs test 0 -eq && echo "CLEAN: No Habbo figure references found"</automated>
  </verify>
  <done>Zero Habbo figure/character references remain in src/, tests/, or scripts/. TypeScript compiles cleanly. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual UAT — verify room rendering with team-specific PixelLab characters</name>
  <files></files>
  <action>
Human verifies the complete Habbo figure removal and PixelLab character replacement.

What was built across Plans 01-03:
- 5 source files deleted (isoAvatarRenderer.ts, avatarOutfitConfig.ts, avatarBuilderPreview.ts, AvatarBuilderModal.tsx, AvatarDebugGrid.tsx)
- 2 test files deleted (isoAvatarRenderer.test.ts, avatarOutfitConfig.test.ts)
- 42 figure asset files deleted (assets/habbo/figures/)
- ~10 files modified to remove Habbo references and wire PixelLab as sole renderer
- 4 team-specific PixelLab characters generated and committed
- CharacterLegendPanel added to room view

Steps to verify:
1. Open the VS Code extension (F5 or `npm run build` then launch)
2. Verify the room renders without errors — agents should appear as PixelLab characters
3. Verify different agent teams show different character sprites:
   - Planning team agents → distinct character (suit/clipboard look)
   - Core Dev team agents → hoodie/beanie look (like existing beanie-hoodie-guy)
   - Infrastructure team agents → builder/ops look
   - Support team agents → headset/help-desk look
4. Verify the character legend panel is visible at bottom-right showing team → character mapping
5. Verify agents walk, idle, spawn, and despawn correctly with their team character
6. Verify clicking an agent does NOT open an avatar builder (builder should be completely gone)
7. Check VS Code developer console for any errors
  </action>
  <verify>Human visual inspection — type "approved" or describe issues</verify>
  <done>Room renders correctly with team-specific PixelLab characters. Character legend panel visible. No avatar builder accessible. No console errors.</done>
</task>

</tasks>

<verification>
- Codebase audit passes with zero Habbo figure references
- Human UAT confirms visual correctness
- No new test failures beyond pre-existing baseline
</verification>

<success_criteria>
- Full codebase audit: zero references to deleted Habbo figure modules
- Human verification: room renders correctly with team-specific PixelLab characters
- Human verification: character legend panel visible and correct
- Human verification: no avatar builder UI accessible
</success_criteria>

<output>
After completion, create `.planning/phases/17.8-remove-copyrighted-habbo-characters/17.8-03-SUMMARY.md`
</output>

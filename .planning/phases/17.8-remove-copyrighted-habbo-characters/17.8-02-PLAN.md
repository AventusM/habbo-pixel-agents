---
phase: 17.8-remove-copyrighted-habbo-characters
plan: 02
type: execute
wave: 2
depends_on: ["17.8-01"]
files_modified:
  - src/isoAvatarRenderer.ts
  - src/avatarOutfitConfig.ts
  - src/avatarBuilderPreview.ts
  - src/AvatarBuilderModal.tsx
  - src/AvatarDebugGrid.tsx
  - src/avatarRendererTypes.ts
  - src/agentTypes.ts
  - src/RoomCanvas.tsx
  - src/webview.tsx
  - src/extension.ts
  - src/avatarManager.ts
  - src/pixelLabAvatarRenderer.ts
  - src/CharacterLegendPanel.tsx
  - tests/isoAvatarRenderer.test.ts
  - tests/avatarOutfitConfig.test.ts
  - scripts/download-habbo-assets.mjs
  - scripts/convert-cortex-to-nitro.mjs
  - assets/habbo/figures/
  - assets/habbo/manifest.json
  - esbuild.config.mjs
autonomous: true
requirements:
  - REM-04
  - REM-05
  - REM-06
  - REM-07
  - REM-08
  - REM-09

must_haves:
  truths:
    - "No Habbo figure/character assets remain in the repository"
    - "No Habbo avatar renderer code remains in src/"
    - "No avatar builder UI is accessible or rendered"
    - "Avatar builder related message types are removed from agentTypes.ts"
    - "AvatarSpec no longer contains Habbo-specific fields (outfit, nextBlinkMs, blinkFrame)"
    - "AvatarSpec has a team field for PixelLab character selection"
    - "Character legend panel shows team-to-character mapping in the room view"
    - "All tests pass (excluding pre-existing 5 failures)"
    - "Figure download/conversion scripts no longer process figure assets"
  artifacts:
    - path: "src/CharacterLegendPanel.tsx"
      provides: "Character legend panel component"
      min_lines: 20
    - path: "src/avatarRendererTypes.ts"
      provides: "Clean AvatarSpec without Habbo fields, with team field"
      contains: "team.*TeamSection"
  key_links:
    - from: "src/RoomCanvas.tsx"
      to: "src/pixelLabAvatarRenderer.ts"
      via: "Direct renderer usage (no habbo fallback)"
      pattern: "pixelLabRenderer"
    - from: "src/RoomCanvas.tsx"
      to: "src/CharacterLegendPanel.tsx"
      via: "JSX component rendering"
      pattern: "CharacterLegendPanel"
---

<objective>
Remove all copyrighted Habbo character/figure code, assets, and UI. Add character legend panel.

Purpose: Eliminate all copyright-infringing Habbo figure content — renderer, builder UI, outfit catalog, figure assets, conversion scripts. Replace with a simple character legend panel showing which team maps to which character. After this plan, the only avatar renderer is PixelLab.

Output: 5 source files deleted, 2 test files deleted, 42 figure asset files deleted, ~10 files modified to remove Habbo references, 1 new CharacterLegendPanel.tsx created.
</objective>

<execution_context>
@/Users/antonmoroz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonmoroz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17.8-remove-copyrighted-habbo-characters/17.8-RESEARCH.md
@.planning/phases/17.8-remove-copyrighted-habbo-characters/17.8-01-SUMMARY.md
@src/avatarRendererTypes.ts
@src/RoomCanvas.tsx
@src/agentTypes.ts
@src/extension.ts
@src/webview.tsx
@src/avatarManager.ts
@src/pixelLabAvatarRenderer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Habbo figure source files, test files, and assets</name>
  <files>
    src/isoAvatarRenderer.ts
    src/avatarOutfitConfig.ts
    src/avatarBuilderPreview.ts
    src/AvatarBuilderModal.tsx
    src/AvatarDebugGrid.tsx
    tests/isoAvatarRenderer.test.ts
    tests/avatarOutfitConfig.test.ts
    assets/habbo/figures/
  </files>
  <action>
**Delete the following source files entirely:**
- `src/isoAvatarRenderer.ts` — Habbo multi-layer avatar renderer
- `src/avatarOutfitConfig.ts` — Figure catalog, presets, color palettes
- `src/avatarBuilderPreview.ts` — Preview canvas for avatar builder
- `src/AvatarBuilderModal.tsx` — Avatar builder UI panel
- `src/AvatarDebugGrid.tsx` — Debug grid for Habbo renderer

**Delete the following test files entirely:**
- `tests/isoAvatarRenderer.test.ts` — Tests for Habbo renderer (26+ tests)
- `tests/avatarOutfitConfig.test.ts` — Tests for outfit config (30+ tests)

**Delete figure assets directory:**
- `rm -rf assets/habbo/figures/` — Remove all 42 figure PNG+JSON files (21 pairs)

**Update assets/habbo/manifest.json:**
- Remove the `"figures"` array entirely from the JSON. Keep only the `"furniture"` array.

**Update scripts/download-habbo-assets.mjs:**
- Remove the `FIGURE_ITEMS` array and its download loop section
- Keep the `FURNITURE_ITEMS` array and furniture download logic intact

**Update scripts/convert-cortex-to-nitro.mjs:**
- Remove the figure conversion section (any code that processes figure/body-part assets)
- Keep the furniture conversion section intact
- If the script becomes empty after removing figure code, delete it entirely

**Update esbuild.config.mjs:**
- Remove any copy step that specifically handles `assets/habbo/figures/` or figure-related files
- Keep furniture asset copy steps intact

After all deletions, run `npx tsc --noEmit 2>&1` to see what import errors remain — these will be fixed in Task 2.
  </action>
  <verify>
    <automated>test ! -f src/isoAvatarRenderer.ts && test ! -f src/avatarOutfitConfig.ts && test ! -f src/AvatarBuilderModal.tsx && test ! -f src/avatarBuilderPreview.ts && test ! -f src/AvatarDebugGrid.tsx && test ! -d assets/habbo/figures && echo "All Habbo figure files deleted successfully"</automated>
  </verify>
  <done>All Habbo figure source files, test files, and assets are deleted. Download/conversion scripts no longer reference figure assets.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up all imports, types, and references across codebase + add CharacterLegendPanel</name>
  <files>
    src/avatarRendererTypes.ts
    src/agentTypes.ts
    src/RoomCanvas.tsx
    src/webview.tsx
    src/extension.ts
    src/avatarManager.ts
    src/pixelLabAvatarRenderer.ts
    src/CharacterLegendPanel.tsx
  </files>
  <action>
**CRITICAL: Follow test statuses throughout. Run `npx vitest run` after each file group to catch regressions early.**

**src/avatarRendererTypes.ts:**
1. Remove `import type { OutfitConfig } from "./avatarOutfitConfig.js";`
2. Remove `outfit?: OutfitConfig;` field from AvatarSpec
3. Remove `nextBlinkMs: number;` field from AvatarSpec
4. Remove `blinkFrame: number;` field from AvatarSpec
5. Add `team?: TeamSection;` field to AvatarSpec (import TeamSection from agentTypes.js)
6. Remove the comment blocks referencing "Habbo/Nitro-specific state"
7. Keep all PixelLab-specific fields and the AvatarRenderer interface unchanged

**src/agentTypes.ts:**
1. Remove `import type { OutfitConfig } from "./avatarOutfitConfig.js";`
2. Remove the `avatarOutfits` message variant from ExtensionMessage type (the variant that carries OutfitConfig data)
3. Remove `saveAvatar` and `loadAvatars` message variants from WebviewMessage type (if they exist)
4. Keep all other message types and TeamSection intact

**src/RoomCanvas.tsx:**
1. Remove `import { habboRenderer } from './isoAvatarRenderer.js';` (or similar Habbo renderer import)
2. Remove `import { AvatarBuilderModal } from './AvatarBuilderModal.js';` (or AvatarBuilderPanel)
3. Remove the renderer selection ternary — replace with: `const activeRenderer = pixelLabRenderer;`
4. Remove all `isBuilderOpen`, `builderAvatarId`, `selectedPart` state variables
5. Remove the `AvatarBuilderPanel`/`AvatarBuilderModal` JSX rendering
6. Remove `avatarOutfits` message handler in the message switch
7. Remove `saveAvatar`/`loadAvatars` webview message posting
8. Remove any `ROLE_OUTFIT_PRESETS` imports
9. Add `<CharacterLegendPanel />` component in the JSX, positioned at bottom-right corner
10. Import `CharacterLegendPanel` from `./CharacterLegendPanel.js`

**src/webview.tsx:**
1. Remove the `nitroFiguresBase` URI variable and its usage
2. Remove the `if (manifest.figures && nitroFiguresBase)` block that loads figure assets via loadNitroAsset
3. Keep the PixelLab atlas loading section (modified in Plan 01)
4. Remove `nitroFiguresBase` from the `ASSET_URIS` injection/parsing

**src/extension.ts:**
1. Remove `nitroFiguresBaseUri` generation code
2. Remove `avatarOutfits` message handler (the one that sends outfit data to webview)
3. Remove `.habbo-agents/avatars.json` persistence logic (read/write of saved outfits)
4. Remove `nitroFiguresBase` from the HTML template injection
5. Keep all PixelLab asset URI generation (from Plan 01)

**src/avatarManager.ts:**
1. Remove `import { getDefaultPreset, getRolePreset } from './avatarOutfitConfig.js';`
2. Remove `import type { OutfitConfig } from './avatarOutfitConfig.js';`
3. Remove outfit assignment logic in `spawnAvatar()` (the lines that set `avatar.outfit = ...`)
4. Remove `nextBlinkMs` and `blinkFrame` initialization (set them to 0 or remove if no longer in AvatarSpec)
5. Update `spawnAvatar()` to set `avatar.team = team || 'core-dev';` directly on the AvatarSpec (instead of using `(spec as any).team` from Plan 01)
6. Remove any `savedOutfits` map and related outfit loading/saving logic
7. Keep the rest of spawnAvatar, despawnAvatar, etc. intact

**src/pixelLabAvatarRenderer.ts:**
1. Update `getAtlasForTeam` to use `spec.team` instead of `(spec as any).team` (now that AvatarSpec has the `team` field)
2. No other changes needed

**src/CharacterLegendPanel.tsx (NEW):**
Create a small React component that renders a character legend panel at the bottom-right of the room view.

Design:
- Dark semi-transparent background (rgba(0,0,0,0.7)) with rounded corners
- Title: "Teams" in Press Start 2P font, 8px size, white
- 4 rows, one per team: a small character sprite (idle frame dir 3) + team label
- Each row: 24x24 character icon + team name (planning, core-dev, infrastructure, support)
- Panel width: ~160px, positioned fixed at bottom-right with 10px margin
- Use the same canvas drawing approach as existing overlay panels, OR use a simple HTML div overlay rendered as a React component alongside the canvas
- Since RoomCanvas uses React, implement as a React component with inline styles
- Character icons: either render small canvas thumbnails from the sprite atlas, or use a static representation (colored dot + label is acceptable as a simpler alternative if atlas access from React is complex)

Keep it simple — this is a read-only legend, not an interactive panel.

After ALL changes, run full test suite: `npx vitest run` — verify test count is approximately 422 - 56 (deleted tests) = ~366 passing, with only the pre-existing 5 failures remaining. No new failures should appear.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All Habbo figure imports and references removed. AvatarSpec cleaned up with team field. Avatar builder UI fully removed. CharacterLegendPanel added. PixelLab is the sole renderer. All remaining tests pass (pre-existing failures excluded).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx vitest run` passes (~366 tests, with only pre-existing 5 failures)
- No file in src/ imports from isoAvatarRenderer, avatarOutfitConfig, AvatarBuilderModal, or avatarBuilderPreview
- No `OutfitConfig` type references remain in the codebase
- `assets/habbo/figures/` directory does not exist
- CharacterLegendPanel renders in the room view
</verification>

<success_criteria>
- Zero Habbo figure/character code or assets in the repository
- PixelLab is the only avatar renderer (no fallback path)
- Avatar builder UI completely removed
- AvatarSpec only contains fields relevant to PixelLab rendering
- Character legend panel visible in room view
- Test suite passes with no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/17.8-remove-copyrighted-habbo-characters/17.8-02-SUMMARY.md`
</output>

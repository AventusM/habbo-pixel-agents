# S01: Capture schema + translators (zeroshot vs loop sections)

**Milestone:** M004
**Slice:** S01

**Goal:** Define the common section/role model and per-run JSONL schema, and build translators that ingest zeroshot run output and gsd-loop artifacts into uniform records.
**Demo:** Translator converts a zeroshot sample run and gsd-loop artifacts (e.g. #79/#92) into uniform per-run JSONL section records; unit tests pass on fixtures.

## Must-Haves

- Translator converts a zeroshot sample run and gsd-loop artifacts into uniform per-run JSONL section records; unit tests pass on fixtures; zeroshot output shape is documented.

## Proof Level

- This slice proves: artifact + unit tests on fixtures

## Integration Closure

Schema and translators are standalone modules with no room or dispatch dependencies; S02 and S03 consume them. No runtime surfaces yet.

## Verification

- None at runtime. Probe logs and the output-shape note are captured under scripts/exp for S02/S03.

<tasks>
- [ ] **T01**: Verify zeroshot probe outcome (PROBE.md on main, PR #96 merged) _(small)_
  Verify the T01 deliverable as merged in PR #96: scripts/exp/PROBE.md exists on main and documents the probe phases, agent roles, section boundaries, log locations, and the zeroshot-OUT verdict (zeroshot --model validation rejects opencode-go/* models). Do NOT re-run the zeroshot probe - that stale contract step is superseded by the probe's own negative finding. If PROBE.md is missing or incomplete, restore it from the PR #96 diff; otherwise record evidence and pass.
  - Files: `scripts/exp/PROBE.md`
  - Verify: test -f scripts/exp/PROBE.md && gh pr view 96 --json state --jq .state | grep -q MERGED
- [ ] **T02**: Verify per-run JSONL schema on main (schema.json) _(small)_
  Verify the T02 deliverable as merged in PR #96: scripts/exp/schema.json parses as JSON, carries required keys run_id/issue/path/model/sections, path enum covers gsd-loop/direct, model documents opencode-go ids, and the evaluation block encodes worker-PR-as-evaluation. Do NOT write a separate ROLES.md - the role/section vocabulary was folded into PROBE.md plus the schema enums by delivery decision. If schema.json is missing or invalid, restore it from the PR #96 diff; otherwise record evidence and pass.
  - Files: `scripts/exp/schema.json`
  - Verify: node -e "const s=JSON.parse(require('fs').readFileSync('scripts/exp/schema.json','utf8')); const miss=s.required.filter(x=>!(x in s.properties)); if(miss.length){console.error('MISSING:'+miss.join(','));process.exit(1)} console.log('schema OK: '+s.required.join(','))"
- [ ] **T03**: Verify translators + fixture tests green on main _(medium)_
  Verify the T03 deliverables as merged in PR #96: scripts/exp/hook-record.mjs (deterministic turn-end emitter: --section spec|build|review|merge, run correlation via --run-id/EXP_RUN_ID, silent skip without) and scripts/exp/translate-loop.mjs (normalizes issue+PR+verdicts to JSONL, validated on #79/#92) exist, then run npx vitest run tests/exp-translate.test.ts tests/exp-hook-record.test.ts and require 6/6 green. translate-zeroshot.mjs was intentionally NOT built (zeroshot disqualified by the T01 probe). If files are missing or tests are red, restore from the PR #96 diff / fix forward; otherwise record evidence and pass.
  - Files: `scripts/exp/hook-record.mjs`, `scripts/exp/translate-loop.mjs`, `tests/exp-translate.test.ts`, `tests/exp-hook-record.test.ts`
  - Verify: npx vitest run tests/exp-translate.test.ts tests/exp-hook-record.test.ts
</tasks>

## Files Likely Touched

- scripts/exp/PROBE.md
- scripts/exp/schema.json
- scripts/exp/hook-record.mjs
- scripts/exp/translate-loop.mjs
- tests/exp-translate.test.ts
- tests/exp-hook-record.test.ts
<!-- gsd:state-version=17:0 -->

# S01: Capture schema + translators (zeroshot vs loop sections)

**Milestone:** M004
**Slice:** S01

**Goal:** Define the common section/role model and per-run JSONL schema, and build translators that ingest zeroshot run output and gsd-loop artifacts into uniform records.
**Demo:** Translator converts a zeroshot sample run and gsd-loop artifacts (e.g. #79/#92) into uniform per-run JSONL section records; unit tests pass on fixtures.

## Must-Haves

- Translator converts a zeroshot sample run and gsd-loop artifacts into uniform per-run JSONL section records; unit tests pass on fixtures; zeroshot output shape is documented.

## Proof Level

- This slice proves: artifact + unit tests on fixtures

## Integration Closure

Schema and translators are standalone modules with no room or dispatch dependencies; S02 and S03 consume them. No runtime surfaces yet.

## Verification

- None at runtime. Probe logs and the output-shape note are captured under scripts/exp for S02/S03.

<tasks>
- [ ] **T01**: Probe zeroshot run output shape _(small)_
  Run a minimal plain-text probe through `zeroshot run` using conductor-bootstrap on Opencode with worktree isolation and no delivery. Capture stdout, cluster logs, and exit artifacts. Record phases, agent roles, section boundaries, and log locations in scripts/exp/PROBE.md.
  - Files: `scripts/exp/PROBE.md`
  - Verify: Probe completes; PROBE.md documents phases, roles, and log paths.
- [ ] **T02**: Define section-role model and JSONL schema _(small)_
  Define the canonical section and agent-role vocabulary mapping zeroshot phases to gsd-loop lanes, plus a JSON schema for per-run records (run, path, section, timestamps, model, tool counts, test and review verdicts). Store the schema and a short role-mapping note under scripts/exp.
  - Files: `scripts/exp/schema.json`, `scripts/exp/ROLES.md`
  - Verify: Schema validates a hand-written sample record.
- [ ] **T03**: Build translators with fixture tests _(medium)_
  Implement translators that convert (a) zeroshot probe and run output and (b) gsd-loop artifacts (schedule GSD_LOOP_RESULT lines, pinned verdicts, UAT saves) into schema-valid section records. Add unit tests on fixtures from the probe and from #79/#92.
  - Files: `scripts/exp/translate-zeroshot.mjs`, `scripts/exp/translate-loop.mjs`, `tests/exp-translate.test.ts`
  - Verify: npx vitest run tests/exp-translate.test.ts is green; translators handle the probe and #79 fixtures.
</tasks>

## Files Likely Touched

- scripts/exp/PROBE.md
- scripts/exp/schema.json
- scripts/exp/ROLES.md
- scripts/exp/translate-zeroshot.mjs
- scripts/exp/translate-loop.mjs
- tests/exp-translate.test.ts
<!-- gsd:state-version=13:0 -->
